<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <title>Claude 3.7 Sonnet 3D x Front-End Demo by Baps Patil</title>

    <!-- Open Graph / Social Media Meta Tags -->
    <meta
      property="og:title"
      content="Claude 3.7 Sonnet 3D x Front-End Demo by Baps Patil"
    />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.url} />
    <meta property="og:image" content={"/public/richlinkpreview.jpg"} />
    <meta
      property="og:description"
      content="A Claude 3.7 Sonnet 3D and Front-End demo with 3D cloth simulation + on-hover spark effects."
    />

    <!-- Twitter Card Tags -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={"/public/richlinkpreview.jpg"} />

    <!-- Google Fonts -->
    <meta
      name="description"
      content="A Claude 3.7 Sonnet 3D and Front-End demo with 3D cloth simulation + on-hover spark effects."
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Figtree:ital,wght@0,300..900;1,300..900&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div id="app">
      <slot />
    </div>
  </body>
</html>

<script>
  // Import GSAP
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";
  import Lenis from "@studio-freight/lenis";

  // Register ScrollTrigger plugin
  gsap.registerPlugin(ScrollTrigger);

  // Initialize smooth scrolling - DISABLED FOR TROUBLESHOOTING
  /*
  const lenis = new Lenis({
    duration: 1.2,
    easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
    smoothWheel: true,
  });

  function raf(time: number) {
    lenis.raf(time);
    requestAnimationFrame(raf);
  }

  requestAnimationFrame(raf);
  */
</script>

<style is:global>
  :root {
    --primary-color: #1e1e1e;
    --secondary-color: #f5f5f5;
    --accent-color: #4f46e5;
    --text-color: #1e1e1e;
    --background-color: #ffffff;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    margin: 0;
    width: 100%;
    height: auto !important;
    overflow-y: scroll !important;
    overflow-x: hidden;
  }

  body {
    margin: 0;
    width: 100%;
    height: auto !important;
    font-family: "Figtree", sans-serif;
    color: var(--text-color);
    background-color: var(--background-color);
    overflow-y: visible !important;
    overflow-x: hidden;
    position: relative;
  }

  #app {
    position: relative;
    width: 100%;
    height: auto;
    overflow: visible !important;
  }

  section {
    position: relative;
    width: 100%;
    min-height: 100vh;
    padding: 2rem;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    font-weight: 700;
    line-height: 1.2;
  }

  h1 {
    font-size: clamp(2.5rem, 5vw, 4rem);
  }

  h2 {
    font-size: clamp(2rem, 4vw, 3rem);
  }

  p {
    font-size: clamp(1rem, 1.5vw, 1.25rem);
    line-height: 1.6;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  button,
  .btn {
    display: inline-block;
    padding: 12px 24px;
    background-color: var(--accent-color);
    color: white;
    border: none;
    border-radius: 4px;
    font-family: "Montserrat", sans-serif;
    font-weight: 600;
    font-size: 1rem;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  button:hover,
  .btn:hover {
    background-color: #3730a3;
    transform: translateY(-2px);
  }

  .hidden {
    opacity: 0;
    visibility: hidden;
  }

  @media (max-width: 768px) {
    section {
      padding: 1rem;
    }

    .container {
      padding: 0 1rem;
    }
  }
</style>
